import random
#print random.randint(0,1)
h = input('Enter a probability: ')
while(1):
	n = input('Enter "n" for next: ')
	if n == "n":
		r = str(random.randint(0,100)
		if r <= str(int(h)-1):
			print ("H")
		else:
			print ("T")		
	
		
